# Set default behaviour, in case users don't have core.autocrlf set.
* text=auto

# These files are text and should be normalized (convert crlf => lf)
*.java text eol=lf
*.xml text eol=lf
*.sh text eol=lf
*.md text eol=lf

# Files that will always have CRLF line endings on checkout.
# Windows expects these to have this ending, and Notepad can't handle other endings.
*.bat eol=crlf
*.cmd eol=crlf

# All files that are truly binary and should not be modified.
# (binary is a macro for -text -diff)
*.jpg binary
*.gif binary
*.png binary
*.bmp binary
*.doc binary
*.class binary
*.rtf binary
*.zip binary
*.jar binary
*.war binary
